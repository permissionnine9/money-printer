### 背景

当前用AI创作视频的方式是先根据给定的长脚本，先用Nano Banana Pro设计2张具有所有素材的图片；然后根据给定的长脚本，人工切片成合适的大小，再基于切片的脚本和素材图片生成切片的首尾帧，然后基于切片的首尾首位帧生产切片的视频；这样多个切片之间能解耦且能保持一定的连贯性；

### 职责

基于当前的背景，现在这套模式很多线下的人工跨平台的操作，效率极低；你需要开发一个视频生成的智能体，完成这一系列的流程；

### 具体目标流程

1. 你需要生成界面，接受用户输入的视频长脚本，以及让用户选定视频参数：分辨率、宽高比、语言、美学风格、视角等，然后点提交；
2. 调用LLM优化用户输入的视频长脚本，优化目标：基于输入的原始长脚本和确定的视频参数，LLM改写并生成高质量的视频脚本，你需要极大的丰富脚本细节，设计8秒以内转场的分片脚本，保障转场之间的连贯性等；
3. 基于生成后的高质量的视频脚本，调用Nano Banana Pro生成包含所有要素的素材图，如果一张放不下，那么可以生成两张；
4. 基于LLM改写后的视频长脚本调用LLM进行分镜头切片，生成分片脚本；注意正文内容、动作、风格、相机位置和运动、构图、对焦和镜头效果、氛围要和总视频、总素材图协调；
5. 基于总素材图和分片脚本，调用Nano Banana Pro生成分镜头的首尾帧图（first\_image、last\_image）两张图片；图片风格、要素要和总素材的风格、分辨率、高宽比保存一致；生成的图片要支持能下载；
6. 基于总素材图+上一步生成分镜头的首尾帧图+分镜头的分片脚本+视频参数，调用veo3.1生成视频；注意：生成的视频注意不要脱离总脚本的故事线；生成的视频要支持能下载；

注意：上面的每一步，我需要变成多轮的形式，也就是每一步直接没有耦合关系，每一步确认好就持久化保存起，当前确认的结果作为下一步运行的时候参数；中间生成的分片镜头可以编辑、分片的首尾帧可以人工删除，AI重新生成和重新人工上传；

### 代码实现建议

1. veo3.1的API参考文档：https://docs.router.shengsuanyun.com/353574235e0
2. 视频和图片用本地文件作为临时存储方案
