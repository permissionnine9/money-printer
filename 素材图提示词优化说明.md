# 素材图提示词优化说明

## 优化概览

### 问题诊断
1. **类型不一致**：任务说明有5种类型，但JSON示例只有3种
2. **图生图干扰**：过多的设计稿元素（标尺、标注）在图生图模式下会干扰生成
3. **类型命名**：原来的类型名称不够清晰，区分不明显

### 优化方案

#### 1. 新的类型系统（5种）

| 类型ID | 名称 | 说明 | 是否必需 |
|--------|------|------|---------|
| `character_main` | 主要角色设定图 | 主角、重要配角 | 通常必需 |
| `character_minor` | 边缘角色设定图 | 龙套、背景人物 | 可选 |
| `props` | 物品/道具设定图 | 重要物品、工具 | 可选 |
| `environment_main` | 主场景设定图 | 核心场景 | 可选 |
| `environment_minor` | 副场景设定图 | 过渡场景 | 可选 |

#### 2. 兼容旧类型

保留对旧类型的支持：
- `character` → 自动映射为 `character_main`
- `environment` → 自动映射为 `environment_main`
- `general` → 通用素材图

#### 3. 模式自适应提示词

根据是否有参考图，使用不同的提示词策略：

**图生图模式（有参考图）：**
- ✅ 侧重描述：外观、风格、氛围
- ✅ 使用通用术语：detailed illustration, concept art, high quality
- ❌ 避免设计稿术语：model sheet, design sheet, height scale ruler, size annotations

**文生图模式（无参考图）：**
- ✅ 可使用设计稿术语：character design sheet, model sheet style
- ✅ 可包含多角度要求：multiple views, front/side/back view
- ✅ 可包含标注要求：height comparison, size annotations

## 代码改动

### 1. LLM服务 (`src/services/llm_service.py`)

#### 改动位置：`generate_material_prompts()` 方法

**优化内容：**
- 更新任务说明，明确5种素材图类型
- 添加图生图/文生图模式的区分提示
- 更新JSON示例，包含所有5种类型
- 优化要求说明，根据模式调整提示词策略
- 保留 `type` 字段传递

**关键变化：**
```python
# 之前
prompts = [
    {
        "prompt": p.get("prompt", "").strip(),
        "description": p.get("description", "").strip()
    }
    ...
]

# 之后
prompts = [
    {
        "prompt": p.get("prompt", "").strip(),
        "description": p.get("description", "").strip(),
        "type": p.get("type", "general").strip()  # 保留 type
    }
    ...
]
```

### 2. 图片服务 (`src/services/image_service.py`)

#### 改动位置：`generate_material_images()` 和 `generate_material_images_with_reference()`

**优化内容：**
- 扩展 `type_names` 字典，支持新类型
- 保持向后兼容，旧类型仍可使用

**类型映射：**
```python
type_names = {
    "character": "角色设定图",          # 兼容旧类型
    "character_main": "主要角色设定图",
    "character_minor": "边缘角色设定图",
    "props": "物品/道具设定图",
    "environment": "场景设定图",        # 兼容旧类型
    "environment_main": "主场景设定图",
    "environment_minor": "副场景设定图",
    "general": "素材图"
}
```

## 使用效果

### 场景1：动画风格视频（有参考图）

**用户操作：**
- 上传一张动漫角色参考图
- 脚本：讲述一个冒险故事，有主角、配角、魔法道具、森林场景

**生成结果：**
1. 主要角色设定图：主角外貌、服装
2. 边缘角色设定图：配角群像
3. 物品设定图：魔法道具
4. 主场景设定图：森林场景

**提示词特点（图生图）：**
- ✅ "A detailed character design of the hero, wearing adventure outfit..."
- ❌ 不包含 "model sheet", "height scale ruler" 等

### 场景2：写实风格视频（无参考图）

**用户操作：**
- 不上传参考图
- 脚本：现代城市职场故事，有主角、办公室、电脑

**生成结果：**
1. 主要角色设定图：主角设定
2. 物品设定图：办公用品
3. 主场景设定图：办公室环境

**提示词特点（文生图）：**
- ✅ "Character design sheet, model sheet style, multiple angles..."
- ✅ "height comparison, professional appearance..."

## 优化亮点

1. **智能模式切换**
   - 自动检测是否有参考图
   - 根据模式调整提示词策略
   - 避免图生图模式下的设计稿元素干扰

2. **灵活类型系统**
   - 支持2-5张素材图
   - 根据脚本内容动态选择
   - 如果某类型不重要，LLM可以省略

3. **向后兼容**
   - 旧类型仍然支持
   - 自动映射到新类型
   - 不影响现有数据

4. **清晰区分**
   - 主要/边缘角色分离
   - 主要/副场景分离
   - 便于生成时控制重点

## 测试建议

### 测试1：图生图模式

```bash
# 准备一张动漫风格的角色参考图
# 脚本：包含主角、配角、道具、场景的故事

预期结果：
- 生成3-5张素材图
- 提示词不包含 "model sheet", "scale ruler" 等
- 生成的素材图风格与参考图一致
```

### 测试2：文生图模式

```bash
# 不上传参考图
# 脚本：简单场景，只有主角和场景

预期结果：
- 生成2-5张素材图（主要角色、主场景）
- 提示词包含 "character design sheet", "multiple views" 等
- 生成的素材图符合设定稿风格
```

### 测试3：复杂场景

```bash
# 上传2-5张参考图（角色、物品）
# 脚本：复杂剧情，包含所有类型元素

预期结果：
- 生成5张素材图（覆盖所有类型）
- 每张素材图类型标注清晰
- 日志中能看到类型区分
```

## 日志验证

**图生图模式：**
```
[步骤3] LLM 生成了 5 个素材图提示词
  - 1. [character_main] 主要角色：英雄，勇敢的冒险者
  - 2. [character_minor] 边缘角色：村民、商人
  - 3. [props] 重要道具：魔法剑、盾牌
  - 4. [environment_main] 主场景：森林深处
  - 5. [environment_minor] 副场景：村庄入口
检测到 2 张参考图，使用 gemini-3-pro-image-preview 模型
生成主要角色设定图 1/5 (使用 gemini-3-pro-image-preview)...
```

**文生图模式：**
```
[步骤3] LLM 生成了 3 个素材图提示词
  - 1. [character_main] 主要角色：现代职场人士
  - 2. [props] 重要道具：笔记本电脑、文件
  - 3. [environment_main] 主场景：现代办公室
生成主要角色设定图 1/3 (文生图模式)...
```

## 常见问题

### Q1: LLM生成的类型不对怎么办？
**A**:
- 检查 LLM 返回的 JSON 格式是否正确
- 查看日志中的 `type` 字段值
- 如果 type 缺失，会自动使用 "general"

### Q2: 为什么有时只生成2-5张？
**A**:
- LLM 会根据脚本内容判断是否需要某类型
- 如果脚本中某类型元素不重要，会自动省略
- 这是正常的智能优化

### Q3: 旧数据会受影响吗？
**A**:
- 不会，旧类型仍然支持
- `character` 会自动显示为"角色设定图"
- `environment` 会自动显示为"场景设定图"

## 下一步优化建议

1. **前端优化**：
   - 步骤3结果展示时，按类型分组显示
   - 添加类型标签（如：🧑主要角色、🔧道具、🏞️场景）

2. **统计优化**：
   - 按类型统计生成数量
   - 显示类型分布（如：主要角色1张、道具1张、场景2张）

3. **提示词模板**：
   - 为每种类型预设提示词模板
   - 用户可以自定义模板

4. **批量重新生成**：
   - 支持只重新生成某个类型的素材图
   - 例如：只重新生成角色设定图，保留其他类型
