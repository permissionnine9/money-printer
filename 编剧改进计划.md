# 「创作剧本」工作流 + 视频工作流重构 实施计划

## Context

当前 money-printer 只有「视频生成工作流」（7 步：提交脚本→优化→思维导图→素材图→分镜脚本→首尾帧→视频），剧本创作完全靠用户手工准备。目标：新增**「创作剧本」菜单（第一个菜单）**，构建 4 步剧本创作工作流，用 Claude Agent SDK（Python 版）驱动每一步，产出结构化的分集设计（含全局人物/场景/线索/伏笔 ID 注册表）；视频工作流砍掉前 3 步 + 素材图移位，入口改为「从剧本选集」，分镜脚本以剧本的分集设计为上下文生成。

### 已确认的决策
- Agent SDK 用 **Python 版 `claude-agent-sdk`**（pip 包，与 FastAPI 同进程；模式照搬 `../annto-knowledge/backend/lib/claude-agent-sdk-wrapper.ts`）实现可复用的组件，供其他功能调用；
- 剧本 4 步**全部用 Agent SDK 作为底层**；视频工作流的全部 LLM 环节（分镜脚本 / 参考图 prompt / 帧 prompt / 分片重生）也切换为 Agent 驱动，**LLMService（盛算云 doubao 链路）彻底退役删除**；
- 首尾帧 prompt 从「每分片并发一次 LLM 调用」改为「**单次 Agent run 批量产出全部分片**」（Agent 每次 run 是一个 CLI 子进程，无法几十个并发）；
- 视频工作流**彻底砍掉** submit_script_and_params / optimize_script / generate_mindmap 三步，旧会话不兼容
- 素材图拆两层：剧本侧新增「**剧本定妆照生成**」（第 4 步，勾选人物/场景实体生成全局设定照）；视频侧原素材图改名「**本集的参考图生成**」移到分镜脚本之后（仅本集细节/辅助场景、辅助人物）
- Agent 模型端点：**模型管理加 `model_type='agent'` 类型**（Anthropic 协议 base_url/api_key/model_id，官方 API 或兼容网关）

---

## 一、数据模型（backend/core/persistence/）

同库 `data/sessions.db`，开 WAL + busy_timeout=5000。

**sessions 表加列**（迁移仿照 `ModelManager._init_database` 的 ALTER TABLE try/except）：
- `workflow_type TEXT DEFAULT 'video'`（`video` | `script`）
- `script_session_id TEXT NULL`、`source_episode_id TEXT NULL`（视频会话指向剧本会话与选中集）

**`SessionManager.STEPS` 改为构造参数**（现有逻辑零改动复用）：

```python
SCRIPT_STEPS = ["story_ideation", "story_outline", "episode_design", "lookbook_images"]
VIDEO_STEPS  = ["select_episode", "generate_segment_scripts",
                "generate_episode_reference_images", "generate_segment_frames", "generate_videos"]
```

**新表**（新模块 `backend/core/persistence/script_manager.py` 建表 + DAO + ID 分配）：

| 表 | 关键字段 |
|---|---|
| `script_entities`（全局实体注册表） | `entity_id PK`（chr_001/scn_001/clu_001/fs_001）、`script_session_id`、`entity_type`（character/scene/clue/foreshadow）、`name`、`description`（人物外貌服装/场景氛围，供生图）、`meta JSON`、`lookbook_image_id` |
| `episodes`（分集设计） | `episode_id`（ep_01）+ UNIQUE(script_session_id, episode_id)、`title`、`logline`、`conflict_chain`、`causality_chain`（≤2000 字）、`ending_summary`（100-300 字，**供下一集分镜上下文**）、`character_ids JSON`、`scene_ids JSON`、`clue_refs JSON`、`foreshadow_refs JSON`（`{entity_id, action: plant|develop|payoff}`，跨集埋设→回收） |
| `lookbook_images`（定妆照） | `image_id`、`script_session_id`、`entity_id`、`prompt`、`description`、`image_path`、`task_status`（沿用素材图状态机 pending/processing/completed/failed） |

**ID 规则**：只由后端分配（`next_entity_id()`：SELECT MAX+1）；agent 通过 MCP 工具注册实体时 handler 分配 ID，引用不存在 ID 返回错误文本让 agent 自纠。

**故事大纲不建表**：存 `step_results('story_outline').result_data = {mindmap, edited, requirements}`，与现有思维导图模式一致。

---

## 二、后端架构

### 2.1 Agent SDK 集成层（新包 `backend/core/agent_sdk/`）

移植 TS 参考实现的模块划分：

| 文件 | 职责 |
|---|---|
| `wrapper.py` | `run_agent(options, on_event)`：基于 `ClaudeSDKClient`（进程内 MCP 工具必须用 Client 而非 query()）；构造 `ClaudeAgentOptions`（system_prompt、resume、permission_mode="bypassPermissions"、include_partial_messages=True、max_turns=40、mcp_servers、setting_sources=[] 隔离、env 注入 agent 模型端点）；SDK 消息流归一为 `AgentEvent`；从 init 消息捕获 session_id（resume 用）；支持 interrupt() 取消 |
| `events.py` | `AgentEvent` 类型（thinking / text_delta / tool_use / tool_result / result / error）+ SSE 序列化 |
| `registry.py` | `AgentRunRegistry`：`{run_id: RunHandle}`，有界事件缓冲（deque，SSE 迟连回放）、cancel |
| `model_env.py` | 从 `ModelManager().get_default_model("agent")` 读 base_url/api_key/model_id → 子进程 env（ANTHROPIC_BASE_URL/ANTHROPIC_AUTH_TOKEN/ANTHROPIC_MODEL） |

**可复用 API（剧本 / 视频 / 未来功能统一调用，不绑定剧本业务）**：
- `run_task(prompt, system_prompt, output_format?, tools?, resume?, on_event)` — 单次结构化生成任务（视频侧各 LLM 环节全用它）
- `run_conversation(message, system_prompt, agent_session_id?, tools?, on_event)` — 多轮会话（剧本第 1 步盘问），内部管理 agent_session_id 的 resume 与新建

### 2.2 SSE 两种形态

- **形态 A（第 1 步对话）**：`POST ideation/message` 直接 `StreamingResponse(text/event-stream)` 请求内跑 agent；客户端断开/finally → `client.interrupt()`
- **形态 B（第 2/3/4 步）**：`POST generate` 立即返回 `{run_id}`（复用 `steps.py` 的 BackgroundTasks + run_async_task 模式）；前端连 `GET runs/{run_id}/events` 观流（缓冲回放+实时+[DONE]）；`POST runs/{run_id}/cancel`。**run 与 SSE 连接解耦**——关页面任务照跑，重开续看

### 2.3 剧本 4 步的 agent 形态（新 `backend/core/agents/script_workflow.py`）

1. **story_ideation 故事构思（多轮对话）**：每轮用户消息一次 agent 运行；首轮 SDK 生成 `agent_session_id` 存 step_results，后续轮 `resume` 之（应用层不维护 messages 上下文，但把每轮消息追加进 `result_data.messages` 供前端刷新还原 UI）。system prompt（`backend/prompts/script_ideation.md`，注入编剧思维：欲望—阻力—选择—代价—变化；每轮只问 1-2 个最关键问题）。「确认故事逻辑」→ `POST ideation/finalize`（SSE）：同一 resume 会话收敛为一段故事逻辑文本，存 `result_data.story_logic`，推进步骤
2. **story_outline 故事大纲（单次 run）**：输入 = 故事逻辑 + 字数/集数/场景数要求；约束 markdown 层级、每节点 ≤100 字、按集数铺一级分支。人工编辑 `PUT outline`（复用 update_mindmap 的不推进不重置语义）；重生成清下游（级联清 episodes/entities/lookbook）
3. **episode_design 分集设计（单次长 run + 工具增量落库）**：注入进程内 MCP server（`create_sdk_mcp_server + @tool`）：`get_context`（大纲+实体清单）、`upsert_character/scene/clue/foreshadow`（handler 分配 ID）、`save_episode`（落库+校验）。**增量可见**：每 save_episode 一行，前端轮询 `GET episodes` 看时间线逐集点亮。handler 强校验：引用存在性、集号连续、伏笔 payoff 集号 > plant 集号、每集 ≥1 人物+1 场景、≤2000 字。**单集重设计** `POST episodes/regenerate`：上下文 = 大纲+实体库+前一集 ending_summary+后一集 logline，覆写该集（episode_id 不变，不破坏下游引用）
4. **lookbook_images 定妆照（agent 出 prompt + 确定性生图）**：`POST lookbook/generate {entity_ids[], style_prompt, model_config_id}`：后台 run 中 agent 单轮产出 `[{entity_id, prompt(英文), description(中文)}]`（校验 entity_id ⊆ 勾选集）；随后同一后台任务按现有素材图异步模式生图（复用 `image_service.submit_i2i_task/poll_i2i_task` + `SHENGSUANYUN_IMAGE2IMAGE_REQUEST_TIME_GAP` 间隔），写 lookbook_images 状态机。单张重生成/图生图编辑复用 ImageService。**步骤完成 = 手动确认** `POST lookbook/complete`（按需勾选无自然终点）

### 2.4 Prompt 管理

沿用 `PromptManager` + `backend/prompts/*.md` + 网页编辑（`api/v1/prompts.py` 自动列出零改动）。新增 5 模板：`script_ideation.md`、`script_ideation_finalize.md`、`script_outline.md`、`episode_design.md`（含工具协议与一致性规则）、`lookbook_prompts.md`。故事逻辑/大纲等变量作为 user prompt 内容（resume 会话保持 system 稳定）。

### 2.5 API 端点（新路由文件，`backend/main.py` 注册；schema 集中 `backend/schemas/script.py`；`deps.py` 加注入）

```
POST/GET/DELETE  /api/v1/script-sessions[.../{sid}]           会话 CRUD（workflow_type='script'）
POST   /script-sessions/{sid}/ideation/message                用户消息（SSE 直跑）
POST   /script-sessions/{sid}/ideation/finalize               收敛故事逻辑（SSE，推进步骤）
GET/PUT /script-sessions/{sid}/ideation                       历史 + story_logic 编辑
POST   /script-sessions/{sid}/outline/generate                → {run_id}
PUT    /script-sessions/{sid}/outline                         人工编辑 markdown
POST   /script-sessions/{sid}/episodes/generate               全量分集设计 → {run_id}
POST   /script-sessions/{sid}/episodes/regenerate             单集重设计 → {run_id}
GET/PUT/DELETE /script-sessions/{sid}/episodes[.../{episode_id}]
GET/POST/PUT/DELETE /script-sessions/{sid}/entities[...]      实体库 CRUD（删除校验 episodes 反向引用）
POST   /script-sessions/{sid}/lookbook/generate|complete      勾选生图 / 手动完成
GET    /script-sessions/{sid}/lookbook[/{image_id}/regenerate|edit]
GET    /api/v1/agent-runs/{run_id}/events                      SSE 观流（**通用端点，不绑剧本会话**，视频侧 run 也用）
POST   /api/v1/agent-runs/{run_id}/cancel                      取消 agent run（通用）
POST   /api/v1/sessions/from-script                           视频会话创建 {script_session_id, episode_id}
```

### 2.6 视频工作流改造（`workflow_v2.py` + `steps.py`）

- **删除**：`step_submit`、`step_optimize_script`、`step_generate_mindmap`、`update_mindmap` 及对应端点 `/submit /optimize /mindmap /regenerate-mindmap /resubmit /reoptimize`
- **新增 `step_select_episode`**：校验剧本会话 episode_design 已完成、episode 存在；保存 `{script_session_id, episode_id, episode_title, video_params}`
- **新增 `backend/core/services/script_context_service.py`（剧本上下文装配器）**，`build_segment_script_context(script_session_id, episode_id)`：
  1. 本集分集设计全文（矛盾链/因果链/人物/场景/线索/伏笔）
  2. 全局大纲摘要（markdown 压缩上限 ~1500 字）
  3. 人物/场景库卡片（被引用实体 description，每个 ≤120 字）
  4. 上一集 `ending_summary`（ep_01 无）
  5. 伏笔关联集摘要（本集 foreshadow_refs 反查其它集的 logline + ending_summary，每集 ≤200 字）
- `step_generate_segment_scripts`：prompt 输入从 optimized_script 换为上述上下文 + `max_segment_duration`；**LLM 调用切换为 `run_task`**（output_format 约束 segments JSON）；更新 `segment_scripts.md` 模板头部上下文段（分片协议、首尾帧模式协议不变）
- `generate_episode_reference_images`（原素材图改名）：上下文 = 分镜脚本 + 实体库；**prompt 生成切换为 `run_task`**；产出仅本集细节/辅助场景、辅助人物（`image_type` 扩展 `detail_scene/aux_scene/aux_character`）；**定妆照注入**：`fetch_lookbook(script_session_id, status='completed')` 合并进素材池（`image_type='lookbook'`），供首尾帧 i2i 参考
- **`step_generate_segment_frames` 的 LLM 环节批量化**：`generate_frame_prompts` 从「每分片并发一次调用」改为「**单次 `run_task` 批量产出**」——输入 = 全部分镜脚本（带前后分片上下文）+ 素材描述，输出 = 所有分片的 `[{segment_index, first_frame, last_frame}]` 大 JSON；超长时分批（如 ≤10 分片/run 顺序执行）。后续 i2i 生图仍按现有并发提交模式不变
- **LLMService 退役**：删除 `backend/core/services/llm_service.py` 及其 `generate_segment_scripts / generate_material_prompts / generate_frame_prompts / batch_regenerate_segments / optimize_segment_prompt_with_images`（optimize/mindmap 方法随旧步骤删）；重生成分片、优化分片 prompt 等端点全部改调 `run_task`；`PromptManager` 保留（md 模板对 Agent 的 system prompt 同样适用）
- 首尾帧/视频两步：步骤名引用同步更新（`_check_frames_completion_with_special_modes` 等）
- 旧会话：`list_sessions` 识别无 `select_episode` 的会话标记 legacy（隐藏或提示不可用），避免 500

---

## 三、前端架构

### 3.1 菜单与路由（`App.tsx` + `MainLayout.tsx`）

- `navItems` 第一项：`{key: '/script', icon: <FormOutlined/>, label: '创作剧本'}`；原 `/` 改名「视频生成工作流」；新路由 `/script → ScriptWorkflowPage`
- 会话 Sider 抽公共组件 `SessionSider`（props：列表/当前项/新建/删除），`/` 与 `/script` 各自渲染，数据源分别为 `sessionStore` / `scriptSessionStore`

### 3.2 剧本页面（`frontend/src/pages/ScriptWorkflowPage.tsx` + `frontend/src/components/script/`）

| 组件 | 要点 |
|---|---|
| `ScriptWorkflowPage` | 4 步向导骨架（复刻 WorkflowPage 的 URL session_id 恢复 + currentStep 同步模式） |
| `StepIdeationChat` | 聊天气泡列表（assistant 内嵌 thinking 折叠）、输入框、fetchSSE 流式渲染 text_delta；「确认故事逻辑」→ finalize SSE → 确认文本卡片（可编辑 PUT）；SSE 中禁用输入 +「停止」按钮 |
| `StepOutline` | markmap 渲染逻辑从 `Step3Mindmap.tsx` 抽公共组件 `frontend/src/components/common/MindmapView.tsx` 共用；生成参数表单（总字数/集数/场景数/extra_prompt）→ run_id → 观流；预览/编辑双模式 + PUT 保存 + 重生成（清下游确认弹窗） |
| `StepEpisodeDesign` | 左侧集时间线（ep_01..n 状态点，轮询 GET episodes 2s）+ 右侧集详情（矛盾链/因果链/结尾摘要可编辑，人物/场景/线索/伏笔带 ID 的 Tag，点击跳实体库）；顶部 Tabs：分集设计/人物库/场景库/线索伏笔 |
| `StepLookbook` | 实体勾选（人物/场景分组 Checkbox 卡片 + 已有定妆照缩略图）+ 风格 prompt + 生图模型下拉（复用 ModelsPage 模式）；结果网格复用 Step3Materials 卡片交互（状态角标、单张重生成/图生图编辑）；「完成本步骤」按钮 |
| `AgentRunProgress` | 通用组件：连 GET runs/{run_id}/events，状态行 + thinking 折叠 + 取消按钮；result 后自动刷新 store |

### 3.3 SSE 客户端（新 `frontend/src/api/sse.ts`，与 axios 轮询并存）

`fetchSSE(url, body, {onEvent, onDone, onError, signal})`：fetch POST + ReadableStream 手动解析 `data:` 行与 `[DONE]`（POST 端点无法用 EventSource）；AbortController 由组件持有。GET 事件端点同实现（避免 EventSource 自动重连重复消费）。

### 3.4 视频页面改造

- `types/index.ts`：`STEP_NAMES` 重写为 5 步；新增 `SCRIPT_STEP_NAMES`、`ScriptSessionDetail`、`ScriptEntity`、`Episode`、`LookbookImage`、`AgentEvent` 类型
- `WorkflowPage.tsx`：Step0=`StepSelectEpisode`（新：剧本会话下拉（须 episode_design 完成）→ 集卡片列表 → video_params 表单（从 Step1Script 迁移）→ POST /sessions/from-script）；后续 4 步组件不变重排；删除 `Step1Script/Step2Optimize/Step3Mindmap` 引用（markmap 逻辑迁入 MindmapView）
- `Step3Materials` → `StepReferenceImages` 改名适配（标题、说明文案、image_type 扩展）
- `api/client.ts` 新增分组：`scriptSessionApi/scriptStepApi/entityApi/episodeApi/lookbookApi/scriptRunApi`（SSE 走 sse.ts）
- 新 `stores/scriptSessionStore.ts` 镜像 sessionStore 结构，与视频 store 独立；唯一交点：StepSelectEpisode 创建视频会话后 `navigate('/?session_id=...')`

---

## 四、实施阶段（每阶段独立验证）

| Phase | 内容 | 验证 |
|---|---|---|
| **0 Agent SDK 基建** | `backend/core/agent_sdk/` 4 文件；pyproject 加 `claude-agent-sdk`；模型管理加 `model_type='agent'`（`model_manager.py` 迁移 + `ModelsPage` 表单） | 手工脚本跑通：一次 agent 运行（流式事件）+ resume 二轮 + interrupt + 进程内 MCP 工具调用 |
| **1 数据层** | `script_manager.py`（建表/DAO/ID 分配）+ `session_manager.py` STEPS 参数化 + `deps.py` 注入 | 单测 next_entity_id 唯一性；步骤推进/清下游对剧本 4 步生效 |
| **2 剧本后端** | `script_workflow.py` + 5 个 prompt 模板 + `schemas/script.py` + 5 个路由文件 + main.py 注册 | curl 走通 4 步全链路（ideation SSE 对话→finalize；outline run→events；episodes run→工具落库→轮询；lookbook 生图→complete） |
| **3 剧本前端** | sse.ts → scriptSessionStore → 路由/菜单/SessionSider → ScriptWorkflowPage + 6 组件 | 浏览器完整走 4 步；刷新还原；SSE 断开重连续看 |
| **4 视频后端改造** | script_context_service.py → workflow_v2 步骤重定义/删旧 → 5 个 LLM 环节切换 `run_task`（含帧 prompt 单 run 批量化）→ 删除 LLMService → steps.py/sessions.py 端点 → 模板更新 | 对 Phase 2 产出的剧本会话走通视频 5 步（mock ComfyUI），全程无 doubao 调用 |
| **5 视频前端改造** | types 重排 → StepSelectEpisode → StepReferenceImages 改名 → 删 3 个旧组件（抽 MindmapView） | 从剧本页选集进入视频页走完 5 步 |
| **6 收尾** | 旧会话 legacy 处理、文案、轮询与 SSE 并存回归、端到端联调 | 全链路回归 |

---

## 五、风险与注意点

1. **Agent SDK 环境前提（Phase 0 先验证）**：pip 包依赖本机 Claude Code CLI + Node ≥18（本机已有）；模型端点必须 Anthropic 协议（模型管理 agent 类型录入官方 API 或兼容网关）——盛算云 OpenAI 兼容网关不能驱动 Agent SDK
2. **SSE 与轮询并存**：uvicorn StreamingResponse 原生支持；后台 run + 缓冲回放使断连不丢任务；注意 dev 模式 `--reload` 会杀后台任务（部署用非 reload）
3. **长任务取消/超时**：ideation 断连在生成器 finally 里 interrupt；步骤 2/3 设 max_turns=40；ProcessError/CLIConnectionError 落库为 `_success=False` 避免前端永久转圈
4. **SQLite 并发写**：WAL + busy_timeout=5000；保持现有短连接短事务模式
5. **ID 一致性**：ID 只由后端分配；handler 强校验；删实体/删集前校验反向引用；单集重设计 episode_id 不变
6. **resume 依赖 SDK 落盘**（`~/.claude/projects/`）：wrapper 实现 resume 失败降级（应用层 messages 副本拼摘要开新 run）
7. **分镜上下文长度**：script_context_service 每段设上限（大纲 1500 / 实体卡 120×N / 关联集 200×N），优先保留 conflict_chain 与 ending_summary 全文
8. **单 run 大输出**：帧 prompt 批量产出的 JSON 可能超长——`frame_prompts.md` 约定按分片顺序逐段输出、wrapper 对 result 做长度保护，分片数 >10 时分批多 run 顺序执行；JSON 截断时按分片 index 增量补跑
9. **成本结构变化**：帧 prompt 是全流程 token 大头，从 doubao 切到 Anthropic 端点后成本显著上升；模型管理 agent 类型务必填对端点，Phase 4 验证时观察单次完整流程的 token 用量

---

## 六、验证方式（端到端）

1. **Phase 0 前置**：`pip install claude-agent-sdk` 后跑手工脚本（agent 运行 + resume + MCP 工具），确认 agent 模型端点可用
2. **后端**：`uvicorn backend.main:app` 起服务，curl 依次打剧本 4 步端点（SSE 用 `curl -N`），检查 SQLite 中 script_entities/episodes/lookbook_images 数据与 ID 一致性
3. **前端**：`npm run dev`，浏览器完整走「创作剧本」4 步（含刷新还原、SSE 断开重连、单集重设计、定妆照勾选生成），再从第 3 步选集跳转视频工作流走完 5 步；**检查浏览器 Console 零运行时错误**
4. **回归**：模型管理/提示词管理页可用；新建 agent 类型模型并设默认
