# Logics 逻辑索引

> 最后更新: 2026-09-19

| 名称 | 文件 | 描述 | 相关功能 | 迭代记录 | tags |
|------|------|------|---------|---------|------|
| Claude Agent SDK 封装逻辑 | [agent-sdk-wrapper.md](agent-sdk-wrapper.md) | ClaudeSDKClient 消息流归一为 AgentEvent，支持多轮 resume、inte | SSE 事件流协议逻辑（AgentEvent 的 seq 缓冲回放/断线重连，见 logics/sse-event-stream.md）；脚本工作流（构思对话、大纲、分集设计）；分镜工作流（分镜大纲等）；模型管理（agent 模型端点配置） | 2026-09-19 初始创建 | AgentSDK, ClaudeSDKClient, AgentEvent, wrapper, 模型端点 |
| ComfyUI 时间轴视频生成逻辑 | [comfyui-video-generation.md](comfyui-video-generation.md) | 构建 5+17n 帧对齐 timeline 注入 ComfyUI 生成长视频，默认 mock 兜底 | 分镜首帧图生成（workflow_v2 前置步骤 `generate_segment_frames`，产出 `segment_frames[].first_image_path`）；会话音频资产（`session_manager.list_assets(session_id, asset_type='audio')`，实现位于 `backend/core/persistence/session_manager.py:663`）；视频工作流全景 — 见 `features/video-workflow.md`；素材上传与资产 — 见 `features/upload-and-assets.md`；文件化工作区（分镜文件读取 `read_storyboard`）— 见 `logics/workspace-store.md` | 2026-09-19 初始创建 | comfyui, 视频生成, timeline, mock |
| 双层持久化与投影逻辑 | [dual-persistence-projection.md](dual-persistence-projection.md) | workspace 文件为权威源、SQLite 只存薄 envelope，投影层还原旧 step_r | 剧本创作工作流（features/script-workflow.md）；workspace 文件化存储核心逻辑（logics/workspace-store.md）；workspace 权威存储规范（project-rules/workspace-authoritative-storage.md） | 2026-09-19 初始创建 | persistence, workspace, sqlite, migration, projection |
| OpenAI 兼容生图服务 | [image-generation-service.md](image-generation-service.md) | OpenAI 兼容生图：配置校验、文生图/图生图、重试退避、同步结果缓存 | 模型管理（image_models 表：默认 image 模型配置、api_key/base_url/model_id）；定妆照 / 分集素材图生成（script_workflow.py、storyboard.py 调用方链路）；静态资源服务（/static 挂载，生成图片对外访问） | 2026-09-19 初始创建 | 生图服务, OpenAI兼容, 图像压缩, 模型配置 |
| 进程内 MCP 写侧工具逻辑 | [mcp-tool-writing.md](mcp-tool-writing.md) | script_design MCP 五写工具，save_episode 强校验失败自纠重试 | 分集生成 generate_episodes（全量 / 单集重设计，见关键流程）；工作区文件化持久化（WorkspaceStore，workspace/ markdown 权威源）；Agent 运行与 SSE 事件流（AgentRunRegistry，另见 `sse-event-stream.md`）；定妆照 / Lookbook（LOOKBOOK_VIDEO_PARAMS：VideoParams(resolution='1080p', aspect_ratio='16:9') 为生图锚点；间隔 IMAGE_REQUEST_TIME_GAP=12 秒提交） | 2026-09-19 初始创建 | MCP, AgentSDK, 分集设计, 强校验自纠 |
| Agent 模型端点环境注入逻辑 | [model-env-injection.md](model-env-injection.md) | build_agent_env 从 SQLite image_models 读 agent 默认模型 | 模型管理页（ModelsPage：image/chat/agent 三类端点 CRUD 与默认互斥）；Claude Agent SDK 封装（wrapper：子进程生命周期、事件归一、resume/interrupt，见 agent-sdk-wrapper.md）；剧本 4 步工作流 / 视频 V2 工作流（所有 agent 步骤的端点寻址都依赖本逻辑）；提示词透明化（prompt 事件 → PromptViewerModal，model 字段来源于此） | 2026-09-19 初始创建 | 模型环境注入, build_agent_env, ANTHROPIC_BASE_URL, ClaudeSDKClient, image_models, ModelManager, AgentModelNotConfiguredError |
| 剧本上下文装配逻辑 | [script-context-assembly.md](script-context-assembly.md) | ScriptContextService 从工作区 markdown 装配分镜上下文（分集设计/实体 | 分镜管理（StepSegmentManagement：分镜形式/overlap 配置、参考素材图编辑、提示词生成弹窗）；video-prompt skill 提示词生成（渐进式披露：skill 规范 + 工作区自主检索）；素材池选择（定妆照 + 本集素材 + 其他集素材）；剧本 4 步工作流（story_ideation → story_outline → episode_design → lookbook_images，产出本服务读取的工作区 markdown 与定妆照 DB 记录） | 2026-09-19 初始创建 | 剧本上下文, ScriptContextService, 分镜提示词, 实体卡, 定妆照, 素材池 |
| 会话步骤状态机逻辑 | [session-step-state-machine.md](session-step-state-machine.md) | 两条 4 步工作流以 SQLite step_results 的 _success 标志驱动线性推进 | 创作剧本工作流（ScriptWorkflow 4 步）；视频创作工作流（VideoCreationWorkflowV2 4 步）；SSE 事件流协议逻辑（剧本第 2-4 步与视频第 2/3 步生成观流；视频第 4 步为轮询例外）；文件化工作区权威存储（DB 薄 envelope 的权威源侧） | 2026-09-19 初始创建 | SessionManager, 步骤状态, 工作流, _success, 级联清空 |
| SSE 事件流协议逻辑 | [sse-event-stream.md](sse-event-stream.md) | SSE 按 seq 增量推送 AgentRun 事件，支持断线重连续传与取消 | 构思对话（StepIdeationChat，POST 直跑 SSE：`/script-sessions/{session_id}/ideation/message`、`/script-sessions/{session_id}/ideation/finalize`）；后台生成类接口（经 `backend/deps.py` 的 `start_agent_run` 启动 agent run 的接口，如分镜大纲 `/steps/{session_id}/storyboard-outline/generate`、脚本大纲 `/script-sessions/{session_id}/outline/generate` 等） | 2026-09-19 初始创建 | SSE, 事件流, 断线重连, AgentRun |
| 文件化工作区存储逻辑 | [workspace-store.md](workspace-store.md) | WorkspaceStore 以 markdown+frontmatter 文件树作为剧本/分镜产物 | 剧本工作流（构思/大纲/分集设计，见 features/script-workflow.md）；分镜工作流（分镜大纲/细分镜，见 features/storyboard-workflow.md）；实体管理（实体卡/定妆照引用，见 features/entity-management.md）；Claude Agent SDK 封装逻辑（story_cwd 提供 run_agent 的 cwd、agent_entry 提供 prompt 注入，见 logics/agent-sdk-wrapper.md） | 2026-09-19 初始创建 | WorkspaceStore, workspace, frontmatter, markdown, 持久化, 并发锁, MAP.md |
