# Logics 逻辑索引

> 最后更新: 2026-09-21

| 名称 | 文件 | 描述 | 相关功能 | 迭代记录 | tags |
|------|------|------|---------|---------|------|
| Claude Agent SDK 封装逻辑 | [agent-sdk-wrapper.md](agent-sdk-wrapper.md) | ClaudeSDKClient 消息流归一为 AgentEvent，支持多轮 resume、interrupt 取消、端点环境注入与 SSE 请求内直跑 | SSE 事件流协议逻辑；脚本工作流；分镜工作流；模型管理（agent 模型端点配置） | 2026-09-19 初始创建; 2026-09-19 自校修订; 2026-09-21 删除 output_format/structured_output 能力与 run_conversation 的 tools 参数；新增 MAX_STREAM_BUFFER_SIZE=16MB 流缓冲上限；新增 direct.py 的 sse_direct_response... | AgentSDK, ClaudeSDKClient, AgentEvent, wrapper, 模型端点 |
| ComfyUI 时间轴视频生成逻辑 | [comfyui-video-generation.md](comfyui-video-generation.md) | 构建 5+17n 帧对齐 timeline 注入 ComfyUI 生成长视频（两段式导入/执行 + UI 工作流落盘远程 + SSH 隧道部署），默认 mock 兜底 | 视频工作流全景（features/video-workflow.md）；素材上传（features/upload-and-assets.md）；文件化工作区（logics/workspace-store.md） | 2026-09-19 初始创建; 2026-09-19 自校修订; 2026-09-21 部署体系 + 两段式链路落地 — 编排层迁至 core/workflows/video_workflow.py；COMFYUI_WORKFLOW_PATH 指向 comfyUI-flow 工程；SSH 隧道/settings API/导入到 ComfyUI/备份恢复/指定段生成... | comfyui, 视频生成, timeline, mock, MiniMaxH3, SSH隧道 |
| 双层持久化与投影逻辑 | [dual-persistence-projection.md](dual-persistence-projection.md) | workspace 文件为唯一权威源、SQLite 只存薄 envelope，step_payload 读投影 + workspace_sections 装配拆分自已删除的 workspace_projection | 剧本创作工作流；workspace 文件化存储核心逻辑；workspace 权威存储规范（project-rules） | 2026-09-19 初始创建; 2026-09-21 workspace_projection.py 删除并拆分为 step_payload（读投影，去 DB fallback、扩展视频侧）+ workspace_sections（agents 层装配收敛）；ScriptManager 影子表退役... | persistence, workspace, sqlite, migration, projection |
| OpenAI 兼容生图服务 | [image-generation-service.md](image-generation-service.md) | OpenAI 兼容生图（配置校验/文生图/图生图/重试退避/同步缓存）+ ImageTaskService 统一三处生图任务状态机（依赖注入与持久层解耦） | 模型管理（image_models 表）；核心素材图 / 分集素材图生成（script_workflow.py、storyboard.py 调用方链路）；静态资源服务（/static 挂载） | 2026-09-19 初始创建; 2026-09-19 自校修订; 2026-09-21 状态机统一 — 新增 ImageTaskService 收敛三处生图状态机（依赖注入 + update_row 回调 + on_completed）；prompt 中文化；素材库配套... | 生图服务, OpenAI兼容, 图像压缩, 模型配置, 生图任务状态机, 核心素材图, 素材库 |
| 进程内 MCP 写侧工具逻辑 | [mcp-tool-writing.md](mcp-tool-writing.md) | script_design MCP 五写工具，save_episode 强校验失败自纠重试 | 分集生成 generate_episodes（全量 / 单集重设计）；工作区文件化持久化；Agent 运行与 SSE 事件流 | 2026-09-19 初始创建; 2026-09-19 自校修正 | MCP, AgentSDK, 分集设计, 强校验自纠 |
| Agent 模型端点环境注入逻辑 | [model-env-injection.md](model-env-injection.md) | build_agent_env 从 SQLite image_models 读 agent 默认模型，构造子进程环境（ANTHROPIC_* 四变量双认证注入）驱动 Claude Code CLI | 模型管理页（ModelsPage）；Claude Agent SDK 封装；剧本 4 步工作流 / 视频 V2 工作流；提示词透明化（prompt 事件 → PromptViewerModal） | 2026-09-19 初始创建 | 模型环境注入, build_agent_env, ANTHROPIC_BASE_URL, ClaudeSDKClient, image_models, ModelManager, AgentModelNotConfiguredError |
| 剧本上下文装配逻辑 | [script-context-assembly.md](script-context-assembly.md) | 剧本侧上下文装配：workspace_sections 共享装配段（工作区段/选集/故事逻辑）+ ScriptContextService 装配分镜上下文（分集设计/实体卡/伏笔关联/大纲摘要）+ step_payload 投影读侧 | 分镜管理（含自动匹配参考素材图）；video-prompt skill 提示词生成；素材池选择；剧本 4 步工作流 | 2026-09-19 初始创建; 2026-09-21 装配逻辑收敛至 services 层 — 新增 workspace_sections.py；投影读侧改 step_payload.py（无 DB fallback）；ScriptContextService 收敛为纯分镜上下文装配（素材池方法迁至 MaterialPoolService）... | 剧本上下文, workspace_sections, ScriptContextService, 分镜提示词, 实体卡, 核心素材图, 素材池, step_payload |
| 会话步骤状态机逻辑 | [session-step-state-machine.md](session-step-state-machine.md) | 两条 4 步工作流以 SQLite step_results 的 _success 标志驱动线性推进、回退与级联清空 | 创作剧本工作流；视频创作工作流；SSE 事件流协议逻辑；文件化工作区权威存储 | 2026-09-19 初始创建; 2026-09-21 4 步定义不变；新增 save_aux_state/rollback_completion，删除 delete_step_result/get_latest_session/is_step_cancelled；ideation 第二完成路径 adopt_story_logic；单分镜完成态 complete_segment... | SessionManager, 步骤状态, 工作流, _success, 级联清空 |
| SSE 事件流协议逻辑 | [sse-event-stream.md](sse-event-stream.md) | SSE 按 seq 增量推送 AgentRun 事件，支持断线重连续传、取消与并发排队（MAX_CONCURRENT_RUNS=5） | 构思对话（POST 直跑 SSE）；后台生成类接口（run_agent_endpoint 提交）；前端全局任务坞（AgentRunDock 消费） | 2026-09-19 初始创建; 2026-09-21 registry 新增并发排队（MAX_CONCURRENT_RUNS=5、status queued→running→done）；AgentEvent 新增 queued/started 与 queue_position；_sse_direct 抽出为 direct.py... | SSE, 事件流, 断线重连, AgentRun |
| 文件化工作区存储逻辑 | [workspace-store.md](workspace-store.md) | WorkspaceStore 以 markdown+frontmatter 文件树作为剧本/分镜产物权威源，原子写+双锁并发防护，返回 dict 与旧 SQLite row 形状一致；ScriptManager 影子表已退役 | 剧本工作流；分镜工作流；实体管理；Claude Agent SDK 封装逻辑（story_cwd/agent_entry） | 2026-09-19 初始创建; 2026-09-21 新方法 + 影子表退役 — 新增 read/set_story_title、entity_references/delete_entity_unreferenced、delete_last_episode；ScriptManager 影子表彻底退役... | WorkspaceStore, workspace, frontmatter, markdown, 持久化, 并发锁, MAP.md |
