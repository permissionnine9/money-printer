# 步骤3【生成素材图】改造说明

## 改造目标
支持用户在网页上上传自定义的主体图片和提示词，使用 `gemini-3-pro-image-preview` 模型进行图生图。

## 改造内容

### 1. 配置文件 (`src/config.py`)
- 新增配置项：`SHENGSUANYUN_MATERIAL_IMAGE_MODEL = "google/gemini-3-pro-image-preview"`

### 2. 图片服务 (`src/services/image_service.py`)

#### 新增方法：
- `_get_size_for_material()`: 获取 gemini-3-pro-image-preview 支持的尺寸 (1K/2K/4K)
- `_image_to_base64()`: 将图片转换为 base64 编码格式
- `_generate_material_image_with_reference()`: 使用 gemini-3-pro-image-preview 模型基于参考图生成素材图
- `generate_material_images_with_reference()`: 批量生成素材图（基于参考图）

#### 修改方法：
- `generate_material_images()`: 增加 `reference_images` 参数，自动判断使用文生图还是图生图模式

### 3. 工作流 (`src/agents/workflow_v2.py`)

#### 修改步骤3方法：
- `step_generate_material_images()`: 增加 `reference_images` 参数，支持传入用户上传的参考图

### 4. 前端界面 (`app_v2_improved.py`)

#### 新增UI组件：
- `reference_images_upload`: File上传组件，支持多张图片上传
- 参考图使用提示文本

#### 修改功能：
- `get_action_button_state()`: 返回值增加 `show_reference_upload` 参数，控制参考图上传组件的可见性
- `execute_next_step()`: 增加 `reference_images` 参数，处理用户上传的参考图并传递给工作流

## 使用方法

### 步骤3执行流程：

1. **首次生成素材图**：
   - 用户可选择上传1~多张参考图（支持JPG、PNG格式）
   - 可选输入自定义提示词，进一步控制生成效果
   - 点击【🖼️ 生成素材图】按钮
   - 系统自动检测：
     - 有参考图 → 使用 `gemini-3-pro-image-preview` 模型（图生图）
     - 无参考图 → 使用默认 `openai/gpt-image-1.5` 模型（文生图）

2. **重新生成素材图**：
   - 步骤3完成后，点击步骤3按钮查看结果
   - 显示【🔄 重新生成素材图】按钮
   - 可重新上传参考图或修改提示词
   - 点击按钮重新生成

## API参数说明

### gemini-3-pro-image-preview 模型参数：
```json
{
    "model": "google/gemini-3-pro-image-preview",
    "prompt": "提示词",
    "images": ["data:image/png;base64,xxx", ...],  // 支持base64编码或URL
    "size": "2K",  // 1K/2K/4K (独有参数)
    "aspect_ratio": "16:9"  // 宽高比
}
```

## 技术亮点

1. **自动模型切换**：根据是否有参考图，自动选择最合适的模型
2. **多图支持**：支持上传多张参考图，AI会综合参考生成素材图
3. **Base64编码**：自动将本地图片转换为base64格式，支持API调用
4. **兼容性**：保持了原有的文生图功能，向后兼容

## 注意事项

1. 参考图建议使用高质量、清晰的图片
2. 参考图数量建议1-3张，过多可能影响生成效果
3. `gemini-3-pro-image-preview` 模型生成时间较长（默认超时180秒）
4. 参考图会转换为base64编码，文件过大可能影响网络传输

## 示例场景

### 场景1：生成动漫风格角色设定图
- 上传：一张动漫角色参考图
- 提示词："更鲜艳的颜色，日系动漫风格"
- 结果：生成风格一致的角色设定图

### 场景2：生成特定物品设定图
- 上传：一张产品图片（如手机）
- 提示词："科技感，未来主义"
- 结果：生成设计稿风格的物品设定图

### 场景3：纯文生图（无参考图）
- 不上传参考图
- 提示词："赛博朋克风格城市场景"
- 结果：使用默认模型生成设定图
