# 素材图提示词优化 - 前后对比

## 核心变化对比

### 1. 类型系统

| 维度 | 优化前 | 优化后 |
|------|--------|--------|
| 类型数量 | 3种（character, props, environment） | 5种（character_main, character_minor, props, environment_main, environment_minor） |
| 角色区分 | 不区分 | 区分主要/边缘角色 |
| 场景区分 | 不区分 | 区分主/副场景 |
| 向后兼容 | N/A | 支持旧类型映射 |

### 2. 提示词策略

| 模式 | 优化前 | 优化后 |
|------|--------|--------|
| 图生图 | 固定使用设计稿术语<br>包含标尺、标注等元素 | 根据模式调整<br>避免设计稿元素 |
| 文生图 | 固定使用设计稿术语 | 保留设计稿术语<br>支持多角度展示 |
| 用户提示 | 无明确说明 | 明确说明模式差异 |

### 3. JSON结构

**优化前：**
```json
{
    "prompts": [
        {
            "type": "character",
            "prompt": "...",
            "description": "角色设定图"
        }
    ]
}
```
❌ 只返回3种类型的示例
❌ type字段未被传递保留

**优化后：**
```json
{
    "prompts": [
        {
            "type": "character_main",
            "prompt": "...",
            "description": "主要角色：英雄"
        },
        {
            "type": "character_minor",
            "prompt": "...",
            "description": "边缘角色：村民"
        }
    ]
}
```
✅ 包含所有5种类型的示例
✅ type字段正确传递
✅ description更具体

## 实际效果对比

### 场景：动画冒险故事（有参考图）

**脚本内容：**
> 英雄带着魔法剑穿越森林，遇到村民和商人，最终到达城堡。

#### 优化前生成

**素材图列表：**
1. 角色设定图 (character)
2. 物品设定图 (props)
3. 场景设定图 (environment)

**提示词示例（角色）：**
```
Character design sheet, model sheet style, the hero with magic sword,
multiple angles (front view, side view, back view),
height scale ruler on the side showing measurements,
white background, clean layout, professional concept art
```

**问题：**
- ❌ 主角和配角混在一起，不清晰
- ❌ 图生图时，"height scale ruler"等元素干扰生成
- ❌ 森林和城堡场景混在一起

#### 优化后生成

**素材图列表：**
1. 主要角色设定图 (character_main)
2. 边缘角色设定图 (character_minor)
3. 物品/道具设定图 (props)
4. 主场景设定图 (environment_main)
5. 副场景设定图 (environment_minor)

**提示词示例（主要角色，图生图模式）：**
```
A detailed character design of the brave hero,
wearing medieval armor, holding a magic sword,
determined expression, adventure style,
detailed illustration, high quality
```

**提示词示例（边缘角色，图生图模式）：**
```
Minor characters from the scene:
villagers and merchants, simple medieval clothing,
background characters, detailed illustration
```

**优势：**
- ✅ 主角单独生成，更突出
- ✅ 配角独立一张，简化处理
- ✅ 图生图模式避免了设计稿元素
- ✅ 森林和城堡分别生成，更清晰

### 场景：现代职场故事（无参考图）

**脚本内容：**
> 办公室职员使用电脑工作，参加会议。

#### 优化前生成

**素材图列表：**
1. 角色设定图 (character)
2. 物品设定图 (props)
3. 场景设定图 (environment)

**提示词示例（角色）：**
```
Character design sheet, office worker, professional attire,
multiple angles, height comparison
```

#### 优化后生成

**素材图列表：**
1. 主要角色设定图 (character_main)
2. 物品/道具设定图 (props)
3. 主场景设定图 (environment_main)

**提示词示例（主要角色，文生图模式）：**
```
Character design sheet, model sheet style,
modern office worker, professional business attire,
multiple angles (front view, side view),
height scale ruler showing 1.75m,
white background, professional concept art
```

**优势：**
- ✅ 文生图模式保留设计稿术语
- ✅ 类型更清晰（只有主要角色，无边缘角色）
- ✅ LLM智能判断是否需要某类型

## 代码改动对比

### LLM服务提示词

**优化前（部分）：**
```python
## 任务说明
生成3张设定稿：

### 第1张：角色设定图
- 展示所有角色
- 多角度展示
- 必须包含身高比例尺
...
```
❌ 没有区分主要/边缘角色
❌ 没有针对图生图的特殊说明

**优化后：**
```python
## 任务说明
生成2-5张素材图：

⚠️ 如果用户提供了参考图，避免设计稿元素

### 1. 主要角色设定图 (character_main)
- 展示主要角色
- 描述外貌特征
- 如有参考图：减少设计稿术语

### 4. 边缘角色设定图 (character_minor) (如需要)
- 展示次要角色
- 简化描述
...
```
✅ 明确区分5种类型
✅ 针对图生图模式特别说明
✅ 支持2-5张灵活生成

### 图片服务类型映射

**优化前：**
```python
type_names = {
    "character": "角色设定图",
    "props": "物品设定图",
    "environment": "场景设定图"
}
```

**优化后：**
```python
type_names = {
    "character": "角色设定图",      # 兼容
    "character_main": "主要角色设定图",
    "character_minor": "边缘角色设定图",
    "props": "物品/道具设定图",
    "environment": "场景设定图",    # 兼容
    "environment_main": "主场景设定图",
    "environment_minor": "副场景设定图",
    "general": "素材图"
}
```

## 用户体验对比

### 步骤3界面显示

**优化前：**
```
✅ 已完成

生成图片数量: 3 张

生成的素材图片请查看下方图片区域。
```

**优化后：**
```
✅ 已完成

生成图片数量: 5 张
- 主要角色设定图 1张
- 边缘角色设定图 1张
- 物品/道具设定图 1张
- 主场景设定图 1张
- 副场景设定图 1张

生成的素材图片请查看下方图片区域。
```

### 日志输出

**优化前：**
```
[步骤3] LLM 生成了 3 个素材图提示词
生成角色设定图 1/3...
生成物品设定图 2/3...
生成场景设定图 3/3...
```

**优化后：**
```
[步骤3] LLM 生成了 5 个素材图提示词
  - 1. [character_main] 主要角色：英雄，勇敢的冒险者
  - 2. [character_minor] 边缘角色：村民、商人
  - 3. [props] 重要道具：魔法剑、盾牌
  - 4. [environment_main] 主场景：森林深处
  - 5. [environment_minor] 副场景：村庄入口
检测到 2 张参考图，使用 gemini-3-pro-image-preview 模型
生成主要角色设定图 1/5...
```

## 性能对比

| 指标 | 优化前 | 优化后 | 说明 |
|------|--------|--------|------|
| 生成数量 | 固定3张 | 2-5张 | 根据脚本动态调整 |
| 角色清晰度 | 混合 | 分离 | 主角更突出 |
| 图生图质量 | 一般 | 优秀 | 避免设计稿元素干扰 |
| 类型识别 | 模糊 | 清晰 | 类型标注明确 |
| 兼容性 | N/A | 完全兼容 | 旧数据正常工作 |

## 总结

### 核心改进

1. ✅ **更细粒度的类型系统**：5种类型 vs 3种类型
2. ✅ **智能模式切换**：根据是否有参考图调整提示词
3. ✅ **向后兼容**：旧类型自动映射，不影响现有数据
4. ✅ **更好的生成质量**：图生图模式避免设计稿元素干扰
5. ✅ **更清晰的日志**：类型标注明确，便于调试

### 建议后续优化

1. 前端按类型分组展示素材图
2. 支持按类型重新生成
3. 添加类型统计可视化
4. 支持用户自定义类型优先级
