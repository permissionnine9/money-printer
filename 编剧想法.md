我想要在系统增加一个「创作剧本」的菜单，放在第一个菜单

  仿照现在的视频生成工作流，现在创作剧本，这也是个工作流。这个工作流需要具备的能力：
  第一，根据用户的简单输入，结合编剧思维，先盘问用户相关事情发展的设计理念。最终，在用
  户说出一个简单的故事逻辑后，你根据这个故事逻辑，第一步：获取用户输入并盘问用户，弄出
  一个简单的故事逻辑。

  第二步：根据用户的要求（比如字数要求、场景数要求等）以及编剧思维，生成一个故事大纲，
  并以思维导图的形式呈现。大纲要求的字数不能多，通常一个大纲节点在100个字以内

  第三步：根据编剧思维以及故事大纲，设计每一集为单元，设计矛盾链、故事线（或者叫因果链）。以及每个矛盾链
  ，或者是每个故事线、因果链上的人物、场景等等，这一块的每个节点控制在 2000
  个字以内吧。另外，帮我设计一下这个节点最终需要沉淀的参数，比如说：
  1. 哪些人物参加了
  2. 事情的这个节点的进展
  3. 场景
  4. 线索
  5. 埋伏伏笔

  你先不用帮我实现这块的能力，先帮我想一下这个事情该怎么去做：

  1. 前端页面该怎么展示，或者说怎么完成这 3 个步骤？
  (a) 第 2 步是以什么形式呈现？是思维导图还是其他什么形式？
  (b) 第 3 步怎么呈现？和思维导图有什么关联？
  (c) 每个节点（每集）是不是要有 ID呢？每个人物是不是要有固定的 ID
  呢？每个场景是不是也要作为固定的 ID 呢？这样的话，在节点和节点之间就能进行 ID
  链接引用呢。
  (d) 另外，我现在的视频生成工作流，前面的 4 步是不是应该砍掉呀？直接拿每一集的输出作为第 5 步（分镜镜头脚本生成）的输入，是不是应该这样呀？

如果是这样的话，那么在生成视频分镜镜头脚本的时候，就需要拿到以下内容：
1. 这一集关联的其他集数；
2. 全局的故事线、故事大纲；
3. 上一集的内容。

  另外，我认为要实现以上功能，肯定要在我的后端服务里面下载一个 Cloud Agent SDK。另外，现在如果是这个形式的话，生成素材图的功能是放到哪里呢？是单独弄一个菜单吗？

因为素材图肯定是全局统一固定的，当然，每一集里面也会有一定的素材图，这是正常的哈。比如小人物、小场景，也需要素材图。所以，视频生成工作流的素材图，是不是应该和现在的第 5 步“分镜镜头脚本生成”换一下顺序啊？也就是说，先生成视频脚本，然后再根据视频脚本生成分镜镜头的其他参考图，对吧？

  claude Agent sdk 的实现方式参考 ../annto-knowledge/backend的实现

  先理解我的意图 帮我出计划