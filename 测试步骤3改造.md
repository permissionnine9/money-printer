# 测试步骤3改造 - 快速指南

## 启动应用

```bash
# 确保已安装依赖
uv sync

# 启动应用（V2版本）
uv run python app_v2_improved.py
```

应用会在 http://localhost:7860 启动。

## 测试流程

### 测试1：纯文生图模式（无参考图）

1. **创建新会话**
   - 点击【创建新会话】按钮

2. **步骤1：提交脚本和参数**
   - 输入测试脚本：
     ```
     一个赛博朋克风格的城市，霓虹灯闪烁，未来科技感。
     ```
   - 点击【📝 提交脚本和参数】

3. **步骤2：优化脚本**
   - 点击【✨ 优化脚本】
   - 等待LLM优化完成

4. **步骤3：生成素材图（纯文生图）**
   - **不上传参考图**
   - 可选：在"自定义提示词"中输入：`鲜艳的色彩，高对比度`
   - 点击【🖼️ 生成素材图】
   - 等待生成完成
   - **预期结果**：使用 `openai/gpt-image-1.5` 模型生成素材图

### 测试2：图生图模式（有参考图）

1. **继续使用上面的会话**，或创建新会话并完成步骤1-2

2. **步骤3：生成素材图（图生图）**
   - **上传1-3张参考图**：
     - 可以是角色图片、物品图片或场景图片
     - 支持JPG、PNG格式
   - 可选：在"自定义提示词"中输入：`保持参考图的风格，增强细节`
   - 点击【🖼️ 生成素材图】
   - 等待生成完成
   - **预期结果**：使用 `gemini-3-pro-image-preview` 模型基于参考图生成素材图

### 测试3：重新生成素材图

1. **步骤3完成后**
   - 点击步骤3按钮【🖼️ 生成素材图】查看结果
   - 会显示【🔄 重新生成素材图】按钮

2. **重新生成**
   - 可以重新上传不同的参考图
   - 修改自定义提示词
   - 点击【🔄 重新生成素材图】
   - **预期结果**：使用新的参考图/提示词重新生成

## 验证要点

### 日志验证

查看应用日志（app_v2_improved.log），确认：

1. **无参考图时**：
   ```
   生成素材图 1/X (文生图模式)...
   ```

2. **有参考图时**：
   ```
   检测到 X 张参考图，使用 gemini-3-pro-image-preview 模型
   用户上传参考图: /path/to/image.jpg
   提交素材图生成任务 (gemini-3-pro): ...
   ```

### API参数验证

在日志中查找API请求，确认：

1. **图生图请求包含**：
   ```json
   {
     "model": "google/gemini-3-pro-image-preview",
     "images": ["data:image/...;base64,..."],
     "size": "2K",
     "aspect_ratio": "16:9"
   }
   ```

2. **文生图请求包含**：
   ```json
   {
     "model": "openai/gpt-image-1.5",
     "size": "1536x1024"
   }
   ```

## 常见问题

### Q1: 上传参考图后不显示
**A**: 刷新浏览器，确保组件已正确加载。

### Q2: 图生图生成失败
**A**:
- 检查参考图格式（仅支持JPG、PNG）
- 检查图片大小（建议<10MB）
- 查看日志中的错误信息

### Q3: 生成时间过长
**A**:
- `gemini-3-pro-image-preview` 模型生成时间较长，默认超时180秒
- 可以在日志中看到"任务处理中..."的进度提示

## API密钥配置

确保 `.env` 文件中配置了：
```bash
SHENGSUANYUN_API_KEY=your_api_key_here
```

## 性能对比

| 模式 | 模型 | 生成时间 | 质量 | 适用场景 |
|------|------|---------|------|---------|
| 文生图 | openai/gpt-image-1.5 | 40-60秒 | 中等 | 无参考图，纯创意生成 |
| 图生图 | gemini-3-pro-image-preview | 120-180秒 | 高 | 有参考图，风格一致性要求高 |

## 测试检查清单

- [ ] 纯文生图模式正常工作
- [ ] 上传单张参考图可以生成
- [ ] 上传多张参考图可以生成
- [ ] 自定义提示词生效
- [ ] 重新生成功能正常
- [ ] 日志输出正确
- [ ] API请求参数正确
- [ ] 生成的素材图风格与参考图一致（图生图模式）

## 下一步

测试通过后，可以继续测试后续步骤（步骤4-6），确保整个工作流正常运行。
